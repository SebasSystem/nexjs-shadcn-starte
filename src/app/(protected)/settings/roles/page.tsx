import { RolesView } from 'src/features/settings/views/RolesView';

export const metadata = { title: 'Roles y Permisos | Configuración' };

export default function SettingsRolesPage() {
  return <RolesView />;
}
