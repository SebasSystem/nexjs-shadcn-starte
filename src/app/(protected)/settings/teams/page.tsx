import { TeamsView } from 'src/features/settings/views/TeamsView';

export const metadata = { title: 'Equipos y Cartera | Configuración' };

export default function SettingsTeamsPage() {
  return <TeamsView />;
}
