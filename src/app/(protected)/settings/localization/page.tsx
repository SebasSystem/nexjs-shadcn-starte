import { LocalizationView } from 'src/features/settings/views/LocalizationView';

export const metadata = { title: 'Localización | Configuración' };

export default function SettingsLocalizationPage() {
  return <LocalizationView />;
}
