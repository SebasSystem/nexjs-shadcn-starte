import { UsersView } from 'src/features/settings/views/UsersView';

export const metadata = { title: 'Usuarios | Configuración' };

export default function SettingsUsersPage() {
  return <UsersView />;
}
