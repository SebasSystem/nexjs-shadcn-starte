import { CustomFieldsView } from 'src/features/settings/views/CustomFieldsView';

export const metadata = { title: 'Campos Personalizados | Configuración' };

export default function SettingsCustomFieldsPage() {
  return <CustomFieldsView />;
}
