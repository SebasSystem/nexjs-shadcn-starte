import { MultiCurrencyView } from 'src/features/sales/views/MultiCurrencyView';

export const metadata = {
  title: 'Multimoneda | CRM',
  description: 'Gestiona las monedas y tipos de cambio del sistema.',
};

export default function MultiCurrencyPage() {
  return <MultiCurrencyView />;
}
