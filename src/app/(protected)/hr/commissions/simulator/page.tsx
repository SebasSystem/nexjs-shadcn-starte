import { SimulatorView } from 'src/features/commissions/views/SimulatorView';

export const metadata = {
  title: 'Simulador de Comisión | CRM',
};

export default function SimulatorPage() {
  return <SimulatorView />;
}
