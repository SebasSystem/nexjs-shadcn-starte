import { SimuladorView } from 'src/features/comisiones/views/SimuladorView';

export const metadata = {
  title: 'Simulador de Comisión | CRM',
};

export default function SimuladorPage() {
  return <SimuladorView />;
}
